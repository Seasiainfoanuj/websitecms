<%= render 'admin/shared/header' %>

<div class="page-container row">
  <%= render 'admin/shared/sidebar' %>

  <div class="page-content">
    <div class="content">
      <div class="page-title">
        <i class="fa fa-code"></i>
        <h3>Vehicle <span class="semi-bold">Filters</span> <%= link_to 'Add New', new_admin_vehicle_filter_path, class: "btn btn-primary btn-mini" %></h3>
      </div>

      <div class="row">
        <div class="col-md-12">
          <div class="grid simple">
            <div class="grid-title no-border">
              <h4>Vehicle <span class="semi-bold">Filters</span></h4>

              <div class="bulk-options">
                <%= form_tag bulk_destroy_admin_vehicle_filters_path, id: "bulk-destroy-form", class: "bulk-action-form" do %>
                  <%= submit_tag "Remove Selected", name: nil, id: "bulk-destroy-btn", class: "btn btn-mini btn-danger" %>
                <% end %>
              </div>
            </div>

            <div class="grid-body no-border">
              <table class="table">
                <thead>
                  <tr>
                    <th style="width:1%" >
                      <div class="checkbox check-default">
                        <input id="checkbox" type="checkbox" value="1" class="checkall">
                        <label for="checkbox"></label>
                      </div>
                    </th>
                    <th width="30%">Title</th>
                    <th>Slug</th>
                    <th>Data Filter</th>
                  </tr>
                </thead>

                <tbody>
                  <%= nested_vehicle_filters @vehicle_filters.arrange(:order => :position) %>
                </tbody>
              </table>

              <%= render 'admin/shared/pagination', model: @vehicle_filters %>
            </div><!-- /grid-body -->
          </div><!-- /grid -->
        </div><!-- /col-md-12 -->
      </div><!-- /row -->
    </div><!-- /content -->
  </div><!-- /page-content -->

</div><!-- /page-container -->

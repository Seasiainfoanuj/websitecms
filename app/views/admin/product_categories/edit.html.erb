<%= render 'admin/shared/header' %>

<div class="page-container row">
  <%= render 'admin/shared/sidebar' %>

  <div class="page-content">
    <div class="content">
      <div class="page-title">
        <i class="fa fa-thumb-tack"></i>
        <h3>Edit Product <span class="semi-bold">Category</span> <%= link_to 'Add New', new_admin_product_category_path, class: "btn btn-default btn-mini" %></h3>
      </div>

      <%= render 'form' %>

    </div><%# /content %>
  </div><%# /page-content %>

</div><%# /page-container %>
